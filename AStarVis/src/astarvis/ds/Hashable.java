package astarvis.ds;

/**
 * Interface for hashable object,
 * to use hashmap objects must be hashable and inherit this interface
 * @author Ilari
 */
public interface Hashable {
    public int hashKey();
}
